export enum Role {
    ADMIN = "ADMINISTRATOR",
    USER = "USER",
}

export enum FormFieldType {
    INPUT = "input",
    EMAIL = "email",
    PASSWORD = "password",
    NUMBER = "number",
    CHECKBOX = "checkbox",
    SELECT = "select",
    PHONE_INPUT = "phone",
    DATE_INPUT = "date",
    SEARCH = "search",
    TEXTAREA = "textarea",
    SKELETON = "skeleton",
    FILE='file',
}